# Lunagraph Documentation

Documentation for [Lunagraph](https://lunagraph.com) — the design canvas that writes real code.

## Development

Install the [Mintlify CLI](https://www.npmjs.com/package/mint) to preview locally:

```
npm i -g mint
mint dev
```

View at `http://localhost:3000`.
